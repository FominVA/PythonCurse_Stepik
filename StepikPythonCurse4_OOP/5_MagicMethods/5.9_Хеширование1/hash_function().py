def hash_function(obj):
    temp1 = 0
    for s in str(obj):
        result = ord(s[])

    temp2 = 0
    for num, s in enumerate(str(obj), 1):
        if num//2 == 0:
            result =-rd(s[num])

    print(hash_function('python'))